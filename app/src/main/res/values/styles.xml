<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Activity transition animations -->
    <style name="AppTheme.Slide" parent="Theme.KidsLearning">
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@anim/slide_in_right</item>
        <item name="android:windowExitAnimation">@anim/slide_out_left</item>
    </style>

    <style name="AppTheme.Zoom" parent="Theme.KidsLearning">
        <item name="android:windowAnimationStyle">@style/WindowAnimationZoom</item>
    </style>

    <style name="WindowAnimationZoom">
        <item name="android:windowEnterAnimation">@anim/zoom_in</item>
        <item name="android:windowExitAnimation">@anim/zoom_out</item>
    </style>
</resources>
